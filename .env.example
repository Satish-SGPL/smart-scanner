# ── Database (Neon PostgreSQL) ─────────────────────────────────────────────────
# Create a free database at https://neon.tech and paste the connection string here
DATABASE_URL=postgresql://neondb_owner:npg_6HlqaJMoi0Fn@ep-lucky-grass-ao95zbnf-pooler.c-2.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# ── NextAuth ───────────────────────────────────────────────────────────────────
# For local dev use http://localhost:3000, for production use your Vercel URL
NEXTAUTH_URL=http://localhost:3000
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
NEXTAUTH_SECRET=ikicoWUVwJvqUSUkyHtn7lUReGl/+qrDRPZCNwNvNr4=

# ── N8N Webhooks ───────────────────────────────────────────────────────────────
# 1) Business card scanner — receives image, returns extracted lead JSON
N8N_WEBHOOK_URL=https://n8n.srv990866.hstgr.cloud/webhook-test/business-card

# 2) Lead → Google Sheets sync — receives saved lead JSON, appends a row
N8N_SHEETS_WEBHOOK_URL=https://n8n.srv990866.hstgr.cloud/webhook/lead-to-sheets
